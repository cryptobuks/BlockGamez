This folder contains all Ethereum Solutions:

To Run:

Add Web3JCore-0.5.2.jar -> (~/BlockGamez/BlockGamez-Solutions/Dependencies)

- All dependencies for this jar are managed by Maven (inside pom.xml): run "mvn install" 
  via intellij terminal: (alt-f12).

- Install and run geth: geth --fast --cache=512 --rpcapi personal,db,eth,net,web3 --rpc --testnet

- Enjoy! - Tief.
